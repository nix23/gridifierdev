// Should store ACTION_NAME and copy of all connectors per that action.
// Or -> operation -> action -> action 
// After each change it should send event, and debugger window should add new entries.
// Per every click all connectors should be redrawn, to see what was happened at that step.